<!-- <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Chat Here</title>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: Arial, sans-serif;
      display: flex;
      flex-direction: column;
      height: 100vh;
      background-color: #f0f2f5;
    }

    header {
      padding: 1rem;
      background-color: #4a90e2;
      color: white;
      text-align: center;
      font-size: 1.5rem;
      font-weight: bold;
    }

    main {
      flex: 1;
      padding: 1rem;
      overflow-y: auto;
      margin-bottom: 70px; /* space for the fixed input bar */
    }

    .chatform{
      display: flex;
      padding: 10px;
      background-color: white;
      border-top: 1px solid #ccc;
      position: fixed;
      bottom: 0;
      left: 0;
      right: 0;
    }

    .messagebox{
      flex: 1;
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 20px;
      font-size: 1rem;
      outline: none;
    }

    .submit-button {
      padding: 10px 20px;
      margin-left: 10px;
      background-color: #4a90e2;
      color: white;
      border: none;
      border-radius: 20px;
      cursor: pointer;
      font-size: 1rem;
      transition: background-color 0.3s ease;
    }

    .submit-button:hover {
      background-color: #357ab8;
    }



    #message-container{
      border: 2px solid black;
      height:100vh;
      width: 100vw;
    }




  </style>
</head>
<body>
  <header>Chat Here</header>

  <div id="message-container">




  </div>

<form class="chatform">

  <div>

    <input type="text" class="messagebox" placeholder="Type your message..." />
    
    <button class="submit-button">Submit</button>

  </div>

</form>








  <script src="https://cdn.socket.io/4.8.1/socket.io.min.js" 
    integrity="sha384-mkQ3/7FUtcGyoppY6bz/PORYoGqOl7/aSUMn2ymDOJcapfS6PHqxhRTMh1RR0Q6+" 
    crossorigin="anonymous">
  </script>


  <script>

    let room;
    const socket=io();
    socket.emit("join room");
    socket.on("joined",function(roomname)

  {

    room=roomname;

  })


  socket.on("message",function(message)
{

      receiveMessage(message)


})



    // writing the code for input box...............................




    const chatform=document.querySelector(".chatform")
    const messagebox=document.querySelector(".messagebox")
   


    chatform.addEventListener("submit",function(event)

  {
    event.preventDefault();

    socket.emit("message",{room:room , message:messagebox.value})
    attachMessage(message)
   
    

  })



  // here message comming from the backend to display in the message_container


  const messagecontainer=document.querySelector(".message-container")



function attachMessage(message) {
    const userMessageContainer = document.createElement('div');
    userMessageContainer.style.display = 'flex';
    userMessageContainer.style.margin = '0.5rem 0';
    userMessageContainer.style.justifyContent = 'flex-end';

    const userMessageDiv = document.createElement('div');
    userMessageDiv.style.backgroundColor = '#3B82F6'; // Tailwind's blue-500
    userMessageDiv.style.color = '#FFFFFF';
    userMessageDiv.style.padding = '0.75rem';
    userMessageDiv.style.borderRadius = '0.5rem';
    userMessageDiv.style.maxWidth = '20rem';
    userMessageDiv.style.wordBreak = 'break-word';

    const userMessageText = document.createElement('p');
    userMessageText.textContent = message;

    userMessageDiv.appendChild(userMessageText);
    userMessageContainer.appendChild(userMessageDiv);
    document.getElementById('message-container').appendChild(userMessageContainer);

    document.querySelector("#message-container").scrollTop = document.querySelector("#message-container").scrollHeight;
}

function receiveMessage(message) {
    const messageContainer = document.createElement('div');
    messageContainer.style.display = 'flex';
    messageContainer.style.margin = '0.5rem 0';
    messageContainer.style.justifyContent = 'flex-start';

    const messageDiv = document.createElement('div');
    messageDiv.style.backgroundColor = '#D1D5DB'; // Tailwind's gray-300
    messageDiv.style.color = '#1F2937'; // Tailwind's gray-800
    messageDiv.style.padding = '0.75rem';
    messageDiv.style.borderRadius = '0.5rem';
    messageDiv.style.maxWidth = '20rem';
    messageDiv.style.wordBreak = 'break-word';

    const messageText = document.createElement('p');
    messageText.textContent = message;

    messageDiv.appendChild(messageText);
    messageContainer.appendChild(messageDiv);
    document.getElementById('message-container').appendChild(messageContainer);

    document.querySelector("#message-container").scrollTop = document.querySelector("#message-container").scrollHeight;
}







  </script>
  


</body>
</html> -->



